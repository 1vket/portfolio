
services:
  portfolio:
    build: .
    ports:
      - "5101:5101"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
    networks:
      - app_network


networks:
  app_network:
    external: true


